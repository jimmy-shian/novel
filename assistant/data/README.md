# assistant/data/

將每本小說的 JSON 檔放在此資料夾。

## 命名規則
檔名須符合小說資料夾名稱，例如：

  diba-yanbixiaosheng.json
  quanzhifashi-luan.json

## 來源
在「閱讀助手」編輯器中點選「儲存匯出」取得 JSON 檔案，然後將其複製到此資料夾。

## 讀者使用
讀者在本機（localhost 或 file://）瀏覽小說章節時，overlay.js 會自動讀取對應的 JSON 並高亮人物名稱。
